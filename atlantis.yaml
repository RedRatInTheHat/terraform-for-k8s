version: 3
autoplan:
  when_modified:
    - "*.tf"
  enabled: true

projects:
  - name: terraform-for-k8s
    dir: .
    workflow: default
    autoplan: true

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
